{
  "name": "@interslavic/razumlivost-monorepo",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "prepare": "husky install && lerna bootstrap",
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "clean": "lerna clean",
    "lint": "lerna run lint",
    "test": "lerna run test"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.4.2",
    "@commitlint/config-conventional": "^17.4.2",
    "cz-conventional-changelog": "^3.3.0",
    "husky": "^8.0.3",
    "lerna": "^6.5.1",
    "semantic-release": "^20.0.2",
    "zx": "^7.1.1"
  },
  "workspaces": [
    "packages/*",
    "packages/presets/*"
  ]
}
